import { Component } from '@angular/core';
import { ActivatedRoute, Router } from '@angular/router';

@Component({
  selector: 'app-reload-map',
  templateUrl: './reload-map.component.html',
  styleUrls: ['./reload-map.component.scss']
})
export class ReloadMapComponent {
  
}
